<!-- recibe un objecto combo en f -->
<% if f.status == "ready" %>
	<div class="combo_box ready">
		<ul class="product_list_area">
			<% f.combo_products.each do |combo_product| %>		
				<%= combo_product.product.name %>
				<span class="combo_product_unit_label">Kg</span>
				<%= combo_product.units %>	
			<% end %>
		</ul>
		<span class="combo_price_label">
			Total: $
		</span>
		<div class="combo_price_label">
			<%= f.price %>
		</div>
		<div class="comment_box">
			<%= f.comment.content %>
		</div>

	</div>
<% elsif f.status == "locked" %>
	<div class="combo_box locked">
		<ul class="product_list_area">
			<% f.combo_products.each do |combo_product| %>		
				<%= combo_product.product.name %>
				<span class="combo_product_unit_label">Kg</span>
				<%= combo_product.units %>	
			<% end %>
		</ul>
		<span class="combo_price_label">
			Total: $
		</span>
		<div class="combo_price_label">
			<%= f.price %>
		</div>
		<div class="comment_box">
			<%= f.comment.content %>
		</div>

	</div>
<% elsif f.status == "received" %>
	<div class="combo_box received">
		<ul class="product_list_area">
			<% f.combo_products.each do |combo_product| %>		
				<%= combo_product.product.name %>
				<span class="combo_product_unit_label">Kg</span>
				<%= combo_product.units %>	
			<% end %>
		</ul>
		<span class="combo_price_label">
			Total: $
		</span>
		<div class="combo_price_label">
			<%= f.price %>
		</div>
		<div class="comment_box">
			<%= f.comment.content %>
		</div>

	</div>
<% end %>



	
